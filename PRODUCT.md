# 思绪 MMind · 产品介绍

> 随手捕捉灵感，AI 帮你整理成文

---

## 产品定位

思绪是一款**个人思维记录与整理工具**。它的核心理念是：记录应该零摩擦，整理交给 AI。

很多想法转瞬即逝——走路时的灵感、睡前的感悟、会议中的观察。思绪让你用最快的方式把它们留下来，无论是打字、语音说话还是录音备忘，全部支持。积累之后，内置的 AI 自动提炼主题、生成文集，把散碎的随想变成有条理的文章。

---

## 核心功能

### 1. 时间流（思绪首页）

所有随想按日期倒序排列，今天的内容一目了然。

- **多媒体卡片**：每条随想显示内容预览、图片缩略图、位置标签、分类标签
- **语音标识**：语音录制的随想带有麦克风角标，可在卡片内直接播放音频
- **转录状态**：语音正在后台转录时显示「转录中」提示，刷新后自动更新为文字内容
- **下拉刷新**：从服务器同步最新数据

### 2. 写随想

支持两种输入模式，满足不同场景：

#### 文字模式
- 自由输入文字内容
- **麦克风语音转文字**：点击话筒按钮，实时语音识别，识别结果自动插入输入框
- **附加图片**：从相册选取最多 6 张图片，内联预览
- **附加位置**：一键获取当前 GPS 位置并反向地理编码（显示区/市名称）
- **分类标签**：从预设标签（创作与灵感、生活观察、技术思考、阅读笔记）中多选

#### 语音备忘模式
- 大号录音按钮，长按录音、松开停止，体验接近原生录音 App
- 录音完成后可选择标签
- 保存后立即创建随想，**后台自动调用 Whisper 模型转录为文字**
- 音频文件保留在本地，随时可以回放

### 3. 随想详情与编辑

点击任意随想卡片进入详情页：

- 查看完整内容、所有图片、位置信息、标签
- 语音随想：页面内嵌播放器，显示播放进度条和时长
- **编辑模式**：点击右上角编辑按钮，修改文字、添加/删除图片、更新位置、切换标签
- **删除**：单条删除，二次确认防误操作

### 4. 批量操作

在时间流界面**长按**任意卡片进入批量选择模式：

- 点击选中/取消单条，支持全选
- **批量整理**：将选中随想交给 AI，定向生成文集
- **批量导出**：导出为 Markdown 文件（含时间戳、内容、标签、位置），手机端调用系统分享，网页端直接下载
- **批量删除**：一次性清理多条，二次确认

### 5. AI 文集

AI 自动（每日定时）或手动触发，分析随想内容，完成两个动作：

1. **主题聚类**：调用 DeepSeek 大模型，从内容中提炼 1–3 个主题词
2. **文章生成**：按主题将相关随想整合成 500–800 字的文集，有标题、有层次

文集详情页：
- Markdown 渲染（支持标题、列表、加粗、段落）
- **关联随想**：底部展示该文集引用的原始随想，一键跳转查看

### 6. 数据洞察

统计面板展示：
- 累计随想总数
- 已生成文集数
- 连续记录天数（打卡 streak）
- 标签分布（按主题分类的占比统计）
- AI 生成的个性化洞察语句

### 7. 地点视图

将所有标注了位置的随想**按地点分组**展示：

- 每个地点显示随想数量
- 点击展开查看该地点下的所有随想
- 无位置的随想归入「未标记地点」分组

---

## 技术亮点

| 能力 | 实现方式 |
|---|---|
| AI 文集生成 | DeepSeek Chat（openai-compatible API） |
| 语音转文字 | OpenAI Whisper base 模型（本地离线运行） |
| 实时语音输入 | expo-av + 系统语音识别 |
| 数据本地持久化 | Zustand + AsyncStorage（离线可用） |
| 图片处理 | expo-image-picker（相册权限） |
| 位置服务 | expo-location + reverseGeocodeAsync |
| Markdown 导出 | expo-file-system + expo-sharing |
| 跨平台 | iOS / Android / Web（Expo SDK 54） |
| 后端 | FastAPI + SQLite，定时任务 APScheduler |

---

## 典型使用场景

**场景一：灵感闪现**
走在路上突然有想法 → 打开 App，语音说出来 → 后台自动转成文字，继续走路

**场景二：睡前整理**
记录了十几条随想 → 长按批量选中 → 点「整理」→ 第二天文集页出现一篇完整文章

**场景三：旅行记录**
每到一个地方附上位置 → 回来后在「地点」Tab 按地点回顾所有记忆

**场景四：写作素材**
积累了大量技术思考 → 批量选中相关条目 → 导出 Markdown → 粘贴到博客

---

## 设计原则

- **零摩擦记录**：打开 App 直接开始写，不需要创建分类或选择目录
- **AI 在后台**：整理、归类、标签全部自动完成，用户不需要手动操作
- **隐私优先**：音频和图片存储在本地设备，不上传云端
- **渐进式呈现**：卡片先展示摘要，详情页再展示全文，避免信息过载

---

*思绪 MMind · 让想法不再消失*
